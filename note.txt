<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>URL-SHORTENER</title>
    <link rel="stylesheet" href="/style.css" />
</head>

<body>
    <div class="container">

        <h1>URL Shortener</h1>

        <form action="/" method="post" class="form">
            <div class="input-group">
                <label for="url">Enter URL:</label>
                <input type="url" name="url" id="url" required />
            </div>

            <div class="input-group">
                <label for="shortCode">Enter Short Code:</label>
                <input type="text" name="shortCode" id="shortCode" />
            </div>

            <button type="submit">Shorten</button>
        </form>

        <h2>Shortened URLs</h2>

        <ul class="url-list">

            <% links.map(({ shortCode, rawUrl }) => { %>
                <li>
                    <div class="left">
                        <a class="short-link" href="<%= origin %>/<%= shortCode %>" target="_blank">
                            <%= origin %>/<%= shortCode %>
                        </a>

                        <span class="original-link">
                            <%= rawUrl.length > 55 ? rawUrl.slice(0, 55) + "..." : rawUrl %>
                        </span>
                    </div>
                </li>
            <% }) %>

        </ul>

    </div>
</body>

</html>



<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>URL-SHORTENER</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <div class="container">
    <h1>URL Shortener</h1>

    <form action="/" method="post" class="form">
      <div class="input-group">
        <label for="url">Enter URL:</label>
        <input type="url" name="url" id="url" required />
      </div>

      <div class="input-group">
        <label for="shortCode">Enter Short Code:</label>
        <input type="text" name="shortCode" id="shortCode" />
      </div>

      <button type="submit">Shorten</button>
    </form>

    <h2>Shortened URLs</h2>

    <ul class="url-list">
      <% (links || []).forEach(function(item) { %>
        <% const shortCode = item.shortCode || item.code || ""; %>
        <% const rawUrl = item.rawUrl || item.url || ""; %>

        <li>
          <div class="left">
            <a class="short-link" href="<%= origin %>/<%= shortCode %>" target="_blank">
              <%= origin %>/<%= shortCode %>
            </a>

            <span class="original-link">
              <%= rawUrl.length > 55 ? rawUrl.slice(0, 55) + "..." : rawUrl %>
            </span>
          </div>
        </li>
      <% }); %>
    </ul>
  </div>
</body>
</html>
